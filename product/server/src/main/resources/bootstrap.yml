spring:
  application:
    name: product
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/