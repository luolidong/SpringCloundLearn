spring:
  application:
    name: order
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/